{{policy}}

IMPORTANT:
- Output MUST be valid JSON (RFC 8259).
- Use double quotes only. No single quotes.
- No trailing commas.
- No comments.
- Do not include any text outside the JSON. No markdown.

You are a senior code reviewer. Use the provided Sonar + Git + coverage context to triage issues and select a small safe refactor scope.

Goals:
1) Create triage list: P0 (blocker/security), P1 (critical), P2 (minor).
2) Select up to {{max_files}} files to patch (safe refactor), prioritizing:
   - files with P0/P1 issues
   - low-risk changes (small diff, covered by tests when possible)
3) Provide for each triage item a short SAFE fix idea.

Return JSON object EXACTLY like:
{
  "selected_files": ["path1", "path2"],
  "items": [
    {
      "priority": "P0|P1|P2",
      "file": "path-or-'-'",
      "rule": "sonar_rule_or_topic",
      "message": "short description",
      "why": "why this matters",
      "safe_fix": "concrete safe refactor idea"
    }
  ]
}

Context JSON:
{{context_json}}
